---
id: REQ-20260829-038-62x6
type: request
title: 사파리에서 문서가 깨진다
summary: 마크다운 렌더링이 사파리에서 깨져 보인다는 제보 — 캡처 없음
goal: 사파리에서도 문서가 다른 브라우저와 같게 그려진다
status: blocked
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
sessions: ["911949e0", "38ad98c8"]
agents: ["sub:frontend-developer:a1151d26", "sub:subagent:a2a2785c", "lead:claude-fable-5-1"]
contributions: [{"actor": "sub:frontend-developer:a1151d26", "item": "038 사파리 렌더링", "started": "2026-08-29T22:30:04+09:00", "ended": "2026-08-30T13:23:20+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a1151d264ae4c1c0f.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:subagent:a2a2785c", "item": "Appending final findings to REQ-20260829-038-62x6", "started": "2026-08-30T00:16:57+09:00", "ended": "2026-08-30T00:16:57+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a2a2785ce4cc9f8cd.jsonl"}, {"actor": "lead:claude-fable-5-1", "item": "재점검 2026-09-06 (REQ-20260905-033-62x6 잔여 일괄): 이 라운드에도 맥이 없다 — 이 기계는 WS", "started": "2026-09-06T00:03:18+09:00", "ended": "2026-09-06T00:03:18+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260829-037-62x6
relates: ["REQ-20260903-006-62x6", "REQ-20260905-033-62x6"]
relates_why: {"REQ-20260903-006-62x6": "같은 진단 한 벌로 이 건의 캡처 재료도 함께 걷는다", "REQ-20260905-033-62x6": "잔여 일괄 진행의 대상"}
tags: ["design", "attached", "png", "image"]
created: 2026-08-29T22:28:15+09:00
status_since: 2026-08-31T16:17:35+09:00
updated: 2026-09-06T00:03:18+09:00
priority: 50
---

## Original

원문(2026-08-29 22:27, 프롬프트 하나에 요청 둘이 섞여 있던 것을 분리): "md 파일 문서 렌더링이 사파리 브라우저에서 깨져서 보인다는 제보가 있다. 캡처는 없고."

앞쪽 절반(맥에서 live 판정)은 부모 REQ-20260829-037-62x6.

**캡처도 사파리도 없다.** 그러니 이 요청은 "고쳤다"를 눈으로 확인할 수 없는 자리에서 시작한다.
짐작으로 고치지 말고, **사파리가 실제로 못 쓰는 문법을 코드에서 찾아 세는 것**부터 해야 한다.
의심할 만한 것들: 정규식 lookbehind(사파리 16.4 이전 미지원 — 문법 오류라 그 스크립트 조각이
통째로 죽는다), `String.replaceAll`, `Array.at`, CSS `:has()`·`text-wrap`, `structuredClone`.
조각으로 가른 지금(REQ-20260829-027) 구문 오류 하나가 조각 하나를 통째로 죽이므로 증상이
"문서만 깨진다"로 보일 수 있다 — 그 가설이 앞선다.

## Notes

### 2026-08-29T22:34:29+09:00 tdd (by nicehugepark)

### TDD 시나리오

사파리가 없으므로 "고쳤다"를 눈으로 못 본다. 대신 **브라우저 없이 확정되는 사실**을 시험으로 잠근다.

- [ ] S1. `web/app/*.js` 전체에 정규식 lookbehind `(?<=`·`(?<!` 가 0건이다 (사파리 16.4 미만은 이것을 **문법 오류**로 다뤄 그 조각 파일 전체가 실행되지 않는다).
- [ ] S2. `web/app/*.js` 전체에 사파리 15.x 가 못 쓰는 신문법이 0건이다 — `String.replaceAll` · `Array.at` · `Object.hasOwn` · `structuredClone` · 논리 대입(`??=` `||=` `&&=`) · `findLast` · `toSorted` 계열 · `Object.groupBy` · 정규식 named group.
- [ ] S3. 예외가 필요하면 그 줄에 이유를 적어야만 통과한다 (금지를 침묵으로 우회할 수 없다).
- [ ] S4. 회귀: `md2html`(attach.js) 이 부르는 `mdTable` 은 `ccrender.js` 에 있다 — 그 조각이 죽으면 **문서 본문이 통째로 안 그려진다**. 이 의존이 살아 있는 한 ccrender.js 는 문법 하한을 지켜야 한다는 사실을 시험이 말한다.
- [ ] S5. 조각 하나가 죽으면 화면이 **침묵하지 않는다** — 어느 파일이 왜 죽었는지 한 줄로 띄운다 (캡처 없는 제보의 다음 왕복을 한 번으로 줄인다).
- [ ] S6. 크롬(headless)에서 지금 되던 문서 렌더가 그대로 된다 — 표·제목·인용·첨부.

### 2026-08-29T23:04:27+09:00 frontend-developer (by nicehugepark)

### 결론 — 짐작이 아니라 한 줄로 좁혀졌다

`web/` 전체(조각 28 + 규칙 13 + 껍데기)를 글자로 훑었다. 사파리가 **문법으로 못 읽는** 것은 딱 한 줄이었고, 그 한 줄이 하필 문서 렌더의 길목이었다.

    web/app/ccrender.js:27   const CCTBL_CELL = /(?<!\\)\|/;

lookbehind 는 JavaScriptCore 가 **사파리 16.4 에서야** 받았다. 그전 사파리는 이것을 런타임 오류가 아니라 **문법 오류**로 다루고, 문법 오류는 그 파일을 통째로 죽인다. 오늘 화면을 조각으로 가른 뒤로 죽는 단위가 곧 조각 하나다.

그리고 그 조각이 문서 렌더의 길목이다:

    web/app/attach.js:195   const tb = mdTable(lines, li, "mdtbl", inline);   ← md2html, 본문의 **모든 줄**에서
    web/app/ccrender.js:52  function mdTable(lines, i, cls, cell){           ← 죽는 자리

즉 사파리 16.4 미만에서는 문서 본문 첫 줄에서 `mdTable` 이 없어 예외가 나고 **본문이 통째로 안 그려진다.** 카드도 그래프도 멀쩡하다 — 증상이 "문서만 깨진다"로 보이는 이유가 이것이다.

### 전수 목록 (파일:줄 · 문법 · 사파리 최소 · 깨지면 보이는 것)

**JS — 조각을 죽이는 것 (문법)**

| 자리 | 문법 | 사파리 | 증상 | 처리 |
|---|---|---|---|---|
| app/ccrender.js:27 | 정규식 lookbehind `(?<!` | 16.4+ | **그 파일 전체 미실행** → 문서 본문 통째로 안 그려짐 | 고침 |

그 외 **0건**이다. 센 것: named group `(?<name>)` · `\k<>` · 논리 대입 `??= \|\|= &&=` · 클래스 private 필드 — 전부 0.

**JS — 그 함수만 죽이는 것 (런타임)**: `replaceAll` · `.at()` · `Object.hasOwn` · `structuredClone` · `findLast/findLastIndex` · `toSorted/toReversed/toSpliced` · `Object.groupBy/Map.groupBy` · `Array.fromAsync` · `RegExp.escape` · `AbortSignal.timeout/any` · `requestIdleCallback` — **전부 0건.** 이 화면의 JS 는 놀랍도록 낮은 문법으로 쓰여 있다.

**CSS — 미달은 있으나 조각을 죽이지 않는다**

CSS 는 모르는 선언·규칙을 **그것만 버린다.** 그래서 "문서가 깨진다"의 원인이 될 수 없다. 실제 미달과 판단:

| 자리 | 문법 | 사파리 | 증상 | 처리 |
|---|---|---|---|---|
| overlay.css:322 · header.css:191 · graph.css:30 · terminal.css:158 | `-webkit-user-select` 누락 | 16.4+ | 선택이 안 됨 — 특히 `.dlgcmd`(한 번 눌러 통째로 잡는 명령)가 막다른 길이 된다 | **고침** (같은 저장소가 terminal.css 118·205 에서 이미 접두를 짝지어 둔 관례) |
| density.css:125 · header.css:193 | `:has()` | 15.4+ | 그 규칙만 버려짐 (포커스 링·비활성 흐림) | 안 고침 — 대체 규칙을 넣으면 두 벌이 된다 |
| graph.css:65,68 | `text-wrap:balance` | 17.5+ | 줄바꿈만 덜 고르다 | 안 고침 |
| calm.css 25 · skins.css 44 · overlay.css 4 | `color-mix()` | 16.2+ | 그 선언만 버려짐 | 안 고침 — 기본 스킨(ledger)의 문서 화면에는 없고, overlay 4곳은 hover·보더 장식 |
| actions.css·calm.css·density.css 3곳 | `overscroll-behavior` | 16+ | 스크롤 전파만 | 안 고침 |
| header.css:192 · skins.css 2곳 | `accent-color` | 15.4+ | 체크박스 색만 | 안 고침 |

`backdrop-filter` · `background-clip:text` · `line-clamp` 은 **이미 `-webkit-` 접두가 짝지어져 있다.** `100dvh` · `@container` · `@layer` · `subgrid` · `field-sizing` · `aspect-ratio` · 최상위 await 는 **0건**.

**날짜 파싱 — 결함 없음(확인함)**

사파리는 `new Date("2026-08-29 12:00")` 처럼 공백으로 이은 문자열을 Invalid Date 로 만든다. 서버의 `local_ts()`(bin/s9:12780)가 정확히 그 모양을 만들고 스트림 줄의 `ts` 로 온다. 그런데 화면은 **이미** 그 자리에서 `.replace(" ", "T")` 로 정규화하고 있었다(`ccrender.js:493`, `audit.js:59-60`). 나머지 `Date.parse`·`new Date` 34곳은 전부 `now_iso()` 가 만든 `T`+오프셋 ISO 를 받는다. 고칠 것이 없어 고치지 않았다.

### 고친 방법 — 동작을 바꾸지 않았음을 실행으로 증명

`ccCells` 의 정규식 split 을 글자를 훑는 `ccSplitCells` 로 바꿨다. 판정은 그대로다: 바로 앞 글자가 `\` 인가. 이 저장소가 `attach.js` 의 맨 경로 규칙(REQ-20260829-008-62x6)에서 이미 고른 답과 같은 방식이다.

옛 정규식과 새 함수를 같은 입력에 돌려 **손으로 고른 21개 + 무작위 200,000개, 차이 0건**을 확인했다(`a\|b` · `a\\|b` · `\|\|\|` · 유니코드 포함). 동작은 한 글자도 안 바뀌었다.

### 되돌아오지 않게 — tests/test_safari_syntax.py (8건, 전부 통과)

- S1 lookbehind 를 **따로** 묻는다 — 이것만이 파일을 통째로 죽이므로 실패 메시지가 달라야 한다
- S2 금지 15종을 `web/app/*.js` 전수로 센다. 문법(파일 사망)과 런타임(함수 사망)을 구분해 적어, 다음 제보를 읽을 때 "어디가 깨졌나"를 좁혀 준다
- S3 예외는 그 줄에 `safari-ok: <이유>` 를 적어야만 통과한다 — 표식만 달고 이유가 비면 거부
- S4 `md2html → mdTable → ccrender.js` 의존을 시험이 기억한다. 의존 자체는 옳다(표 규칙이 두 벌로 갈리지 않게 일부러 한 곳에 뒀다) — 막는 것은 **그 의존을 잊는 것**이다
- S5 a~d 알리는 조각의 계약: 맨 앞에 설 것 · ES5 일 것 · 남의 조각을 안 부를 것 · 무엇을 말하는지

주석과 문자열은 세지 않는다. 이 저장소의 주석은 길고, 금지를 **설명하는 글**이 위반이 되면 시험이 자기 설명에 걸려 넘어진다(`attach.js` 에 실제로 그런 문단이 있다).

### 첨부한 그림 — 조각이 죽으면 이제 화면이 말한다

이 요청의 진짜 비용은 그 한 줄이 아니라 **아무도 아무 말도 하지 않았다**는 것이다. 조각 하나가 죽어도 나머지 27개는 멀쩡히 돌고, 껍데기의 "조각을 못 받았습니다" 알림은 **하나도 안 왔을 때**만 본다 — 조각 하나의 죽음은 그 알림의 눈 밖이었다.

그래서 `web/app/oops.js` 를 **맨 앞** 조각으로 세웠다. 죽은 조각의 파일·줄·오류를 한 상자에 모으고, 그 브라우저가 무엇을 할 줄 아는지(lookbehind·`??=`·`replaceAll`·`.at()`·`:has()`·`color-mix`)를 직접 물어 ✓/✗ 로 적고, UA 를 붙인다. 이 파일만은 **ES5** 로 썼다 — 문법으로 죽은 것을 알리는 파일이 문법으로 죽으면 알릴 사람이 없다. 다른 조각의 함수도 안 부르고 스타일도 인라인이다(없어진 것이 하필 그 조각일 수 있으므로). 색은 토큰으로 묻되 `var(--panel, #fff)` 처럼 떨어질 자리를 줘, 규칙이 통째로 안 온 상태에서도 읽힌다.

크롬으로 네 처지를 결정적으로 확인했다(조각을 골라 404 내는 시험용 서버를 세워서):
- 죽은 JS 조각 → 파일·줄·`SyntaxError` 를 그대로 적는다 (첨부 그림, carbon 톤)
- 죽은 CSS 조각 → `css/docs.css — 모양 파일을 못 받았습니다`. 판정은 `sheet` 가 null 인가가 **아니다**: 404 를 받은 `<link>` 도 sheet 객체는 가진다(규칙 0개짜리 빈 시트 — 크롬에서 직접 재 봤다). 그래서 규칙 수를 센다
- 다 멀쩡 + `?oops` → 초록으로 서서 지원 목록만 적는다
- 다 멀쩡 + 파라미터 없음 → **아무것도 안 그린다** (오탐 0)

### 크롬 회귀 — 지금 되던 것이 그대로 된다

문서 화면(`#docs/REQ-20260825-056-62x6`)을 헤드리스 크롬으로 열어 DOM 을 읽었다: `.md` 판 · `mdtbl` 표 2개 · `<h2>/<h3>` 제목이 그대로 그려지고, 표는 13행·6행으로 칸이 정확히 갈렸다(`\|` 가 든 칸 포함). `-webkit-user-select` 추가와 배너는 기존 화면을 밀지 않는다.

### 확신이 없는 것 — 정직하게

1. **사파리에서 확인하지 못했다.** 이 자리에 사파리가 없고 증상 캡처도 없다. 위의 것은 전부 크롬에서 확인한 사실 + 사파리의 문법 하한이라는 문서화된 사실의 결합이다.
2. **친구의 사파리가 16.4 이상이면 lookbehind 는 원인이 아니다.** 그 경우 `color-mix`(16.2+)도 통과하므로 CSS 후보도 남지 않는다 — 배너의 UA 한 줄이 답을 준다.
3. **조각 유실도 같은 증상을 낸다.** 오늘 살아 있는 9909 에서 조각 요청이 30회 중 5회 실패하는 것을 실제로 봤다. 다만 그때 다른 작업이 `s9 serve --restart` 를 돌리고 있었으므로 제품 결함이라 단정하지 않는다. 배너가 이제 그것도 말한다.
4. **알리는 조각 자신이 안 올 수도 있다.** 그때는 껍데기의 기존 알림만 남는다.

### 친구에게 그대로 보낼 한 줄

> 대시보드 주소 끝에 `?oops` 를 붙여 다시 열어 주세요 (예: `http://127.0.0.1:9909/?oops`). 맨 위에 뜨는 상자를 통째로 캡처해 보내 주시면 됩니다 — 그것만으로 원인이 잡힙니다.

### 만진 것

`web/app/ccrender.js`(lookbehind 제거) · `web/app/oops.js`(새 조각) · `web/app/diag.js`(`?oopsfake=` 재현자) · `web/index.html`(맨 앞 한 줄) · `web/css/{overlay,header,graph,terminal}.css`(`-webkit-user-select` 4곳) · `tests/test_safari_syntax.py`(새 시험). `bin/s9` 는 건드리지 않았다.

[Image: assets/REQ-20260829-038-62x6/oops-dark.png]

### 2026-08-30T00:16:51+09:00 frontend-developer (by nicehugepark)

### 마무리 — 전수 최종 수치와, 내가 깨뜨린 시험 하나

앞 노트의 조사·수정에 이어 남은 것을 마쳤다. 조각은 지금 `app/*.js` 28개 · `css/*.css` 13개다(다른 손이 `css/tidy.css` 를 더했다 — 그 조각에도 위반은 없다).

### 최종 전수 (지금 상태로 다시 셈)

**JS — 조각을 통째로 죽이는 문법: 0건.** lookbehind · named group · `\k<>` · 논리 대입 `??= ||= &&=` 전부 0.
**JS — 함수만 죽이는 신문법: 0건.** `replaceAll` · `.at()` · `Object.hasOwn` · `structuredClone` · `findLast` · `toSorted` 계열 · `Object.groupBy` 전부 0.

글자 검색에는 3건이 걸리는데 셋 다 **위반이 아니다**:

| 자리 | 걸린 글자 | 왜 괜찮은가 |
|---|---|---|
| app/oops.js:104 | `(?<!a)b` | `new Function("return /(?<!a)b/")` 안의 **문자열**이다. 파일 자체는 lookbehind 문법을 안 쓴다 — 사파리는 이 파일을 정상으로 읽고, `new Function` 이 던지면 try/catch 가 받아 그 브라우저가 lookbehind 를 못 쓴다고 **적어 준다**. 진단이 그 문법을 쓰지 않고 그 문법을 묻는 방법이다. |
| app/oops.js:107 | `.at()` | 화면에 찍는 **라벨 문자열**이다. 실제 판정은 `typeof [].at === "function"` — 호출이 아니라 존재 확인이라 사파리 15 에서도 안 던진다. |
| app/ccrender.js:33 | `/(?<!\\)\|/` | 무엇을 왜 내렸는지 적은 **주석**이다. |

시험은 주석과 문자열을 걷어내고 세므로 초록이다 — 금지를 **설명하는 글**이 위반이 되면 시험이 자기 설명에 걸려 넘어진다.

**CSS — 조각을 죽이는 것은 없다** (모르는 선언·규칙은 그것만 버려진다): `:has()` 2 · `text-wrap:balance` 2 · `color-mix()` 74 · `overscroll-behavior` 3 · `accent-color` 3. `@container`·`@layer`·`subgrid`·`field-sizing`·`dvh` 는 0건. `-webkit-` 접두가 빠진 자리는 이제 **0**(4곳 고침).

### 내가 깨뜨린 시험 하나 — 고쳤다

전체 스위트(2253건)에서 `test_terminal_table.test_escaped_pipe_stays_a_character` 가 깨져 있었다. **내 변경이 원인이다**: 그 시험이 `const CCTBL_CELL = /…/` 라는 **정규식 상수의 모양**을 붙잡고 있었는데, 내가 그 상수를 없애고 글자를 훑는 `ccSplitCells` 로 내렸다.

시험의 뜻(`\|` 는 칸을 안 가른다)은 옳으므로 뜻은 그대로 두고 **붙잡는 자리만** 옮겼다: 판정 두 줄(`t[i] === "|"` · `t[i-1] !== "\\"`)이 있는지 보고, 같은 규칙을 파이썬으로 옮겨 `a\|b|c` → 2칸 · `a|b|c` → 3칸을 확인한다. 시험이 구현의 **모양**을 붙잡고 있으면 같은 뜻으로 내려 쓰는 것조차 빨간불이 된다 — 이 건이 그 실례다.

### 곁에서 한 일 (REQ-20260829-039-62x6 의 ②)

같은 증상을 내는 두 번째 원인을 재다가 확인했다: 가르기가 F5 한 번을 요청 **42개**로 만들었고, WSL2 루프백은 그 동시 도착에서 무너진다. 살아 있는 9909 에 42개를 한 순간에 걸어 **24/42 · 12/42 · 9/42**. 404 가 아니라 연결이 안 선다. 규칙 조각 하나만 잘려도 레이아웃이 통째로 무너지므로 **사파리와 무관하게 같은 제보를 만든다.**

실패한 것만 120·320·800ms + 지터로 다시 걸면 **41/42 · 42/42 · 42/42** 로 돌아온다. 그 되찾기를 껍데기 `<head>` 맨 앞(링크보다 먼저)에 넣었다. 규칙 조각은 **같은 `<link>` 에** 다시 건다 — 새 링크를 뒤에 붙이면 "뒤가 앞을 덮는다"는 이 껍데기의 계약이 그 조각만 뒤집힌다. 로직 조각은 `async=false` 로 원래 자리 옆에 꽂고, 늦게 온 조각이 도착했는데 판이 비어 있으면 `boot()` 을 다시 부른다(되찾아 놓고 사람이 F5 를 눌러야 하면 되찾은 것이 아니다). 크롬 실측: 6개가 잘린 회차에서 전부 되찾아 판이 그려졌고 붉은 상자는 안 떴다.

`oops.js` 는 되찾는 중인 조각을 죽었다고 말하지 않는다 — 200ms 뒤 돌아올 조각을 두고 사람이 붉은 상자를 캡처해 보내면 진단이 거짓말을 하는 것이다. 세 번 걸어도 못 받은 것만 말한다.

### 못 한 것

- **묶음 한 갈래(①)**: `/css/all.css`·`/app/all.js` 가 아직 404 다(서버 갈래 미착). 42를 2로 줄이는 진짜 답은 그쪽이고, 껍데기가 지금 다른 작업의 리스에 잠겨 있어 부르는 쪽도 못 고친다. 다음 라운드.
- **사파리에서 확인하지 못했다.** 위의 것은 크롬 실측 + 사파리 문법 하한이라는 문서화된 사실의 결합이다. 친구의 사파리가 16.4 이상이면 lookbehind 는 원인이 아니고, 그때는 진단 상자의 UA 한 줄이 답을 준다.

### 돌린 시험 (숫자 그대로)

- `python3 tests/ safari_syntax` — **8건 OK**
- `python3 tests/ terminal_table` — **14건 OK** (고친 뒤)
- `python3 tests/ web_split` — **15건 OK**
- 전체 `python3 tests/` — 2253건, 실패 4·에러 3. 그중 내 것은 위 `terminal_table` 하나(고침)와 `web_split.test_s5`(그 스위트가 내 시험 갱신 **전에** 출발했다 — 지금 15/15 초록). 나머지 다섯(`graph_labels` 2 · `whoami` · `stall_visible` · `web_split.test_s6c_every_part_is_reachable`)은 내 변경과 무관하며, 특히 `s6c` 는 조각이 안 온다는 것이라 위에 적은 42-요청 벼랑 그 자체다.

### 친구에게 그대로 보낼 한 줄

> 대시보드 주소 끝에 `?oops` 를 붙여 다시 열어 주세요 (예: `http://127.0.0.1:9909/?oops`). 맨 위에 뜨는 상자를 통째로 캡처해 보내 주시면 됩니다 — 그것만으로 원인이 잡힙니다.

### 2026-08-30T00:16:57+09:00 subagent (agent: sub:subagent:a2a2785c, by nicehugepark)

Appending final findings to REQ-20260829-038-62x6

### 2026-08-30T01:41:05+09:00 commit (by nicehugepark)

커밋 3c63e9c — 조각 마흔둘을 셋으로 묶고, 끝난 일에는 작업자가 뜨지 않는다 (REQ-039·038·037·030·002)

### 2026-08-30T13:21:35+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T13:23:13+09:00 response (by nicehugepark)

### 이어받아 확인만 했다 — 고침은 이미 들어가 있었고 커밋됐다 (리드)

깨우기로 이어받아 남은 것을 봤다. **새로 고칠 것은 없었다.** 00:16 frontend-developer 의
작업이 그대로 살아 있고 `3c63e9c` 로 커밋돼 있다. 이 세션이 한 것은 그것이 지금도 서는지
실물로 확인하고 판정에 올린 것이다.

**확인한 것.**

- `web/app/ccrender.js` 에 lookbehind 가 **코드에는 0건**이다(주석에 경위로만 남아 있다).
  그 한 줄이 사파리 16.4 미만에서 문법 오류가 되고, 문법 오류는 그 조각을 통째로 죽인다.
  같은 조각에 `mdTable` 이 있어 문서 본문이 첫 줄에서 무너진다 — 증상이 "문서만 깨진다"로
  보이던 이유다.
- `python3 tests/ safari_syntax terminal_table web_split asset_retry bare_asset_path`
  **71건 OK**.
- **`?oops` 진단이 실제로 뜬다.** `http://127.0.0.1:9909/?oops` 를 찍어 눈으로 봤다:
  「화면 조각은 모두 살아 있습니다」 + 지원 표(정규식 lookbehind · 논리 대입 `??=` ·
  `replaceAll` · `.at()` · CSS `:has()` · CSS `color-mix`) + UA 한 줄. 크롬에서는 전부 ✓ 다.
  사파리에서 열면 ✗ 가 찍히는 항목과 죽은 조각의 파일·줄이 같은 상자에 모인다.

**아직 확인하지 못한 것 — 사파리가 이 자리에 없다.**

위는 "크롬 실측 + 사파리 문법 하한"의 결합이지 사파리에서 됐다는 증명이 아니다.
그래서 이 건의 판정은 친구분의 한 번에 달려 있고, `?oops` 는 그 한 번을 **왕복 한 번으로
끝내려고** 만든 것이다. 그 진단 파일만 ES5 로 썼다 — 문법으로 죽은 것을 알리는 파일이
문법으로 죽으면 알릴 사람이 없다.

친구분 사파리가 16.4 이상이면 lookbehind 는 원인이 아니고, 그때는 `?oops` 상자가 어느
조각이 왜 죽었는지 대신 답한다.

[Image: assets/REQ-20260829-038-62x6/s-038-oops.png]

### 2026-08-30T13:23:20+09:00 (agent: sub:frontend-developer:a1151d26, by nicehugepark)

고침·시험·진단(?oops)이 모두 디스크에 남아 있고 커밋 3c63e9c 에 들어갔다. 리드가 이어받아 실물로 확인했다 — 코드에 lookbehind 0건, 관련 시험 71건 OK, ?oops 상자가 실제로 뜬다.

### 2026-08-30T13:24:45+09:00 tdd (by nicehugepark)

### TDD S1~S6 — 전부 검증 완료 (체크 갱신, 리드가 이어받아 확인)

시험: `python3 tests/ safari_syntax terminal_table web_split asset_retry bare_asset_path`
**71건 OK**. 구현은 커밋 `3c63e9c` 에 이미 들어가 있다.

- [ ] S1. `web/app/*.js` 에 정규식 lookbehind 가 **코드에는 0건**이다. `ccrender.js` 의
      `/(?<!\\)\|/` 를 글자 훑기로 내렸고, 옛 정규식과 새 함수를 손으로 고른 21개 +
      무작위 20만 개에 돌려 차이 0건을 확인했다. 남은 `?<` 한 줄은 **주석의 경위 인용**이다.
- [ ] S2. 사파리 15.x 가 못 쓰는 신문법 0건 — `replaceAll`·`.at()`·`Object.hasOwn`·
      `structuredClone`·논리 대입·`findLast`·`toSorted` 계열·`Object.groupBy`·named group.
      글자로 걸리는 몇 건은 전부 문자열/주석(진단의 기능 탐지 라벨)이다.
- [ ] S3. 예외는 그 줄에 이유를 적어야만 통과한다 — 침묵으로는 못 지나간다.
- [ ] S4. `md2html`(attach.js) → `mdTable`(ccrender.js) 의존을 시험이 기억한다.
      이 의존 때문에 조각 하나의 문법 오류가 **문서 본문 전체**를 무너뜨렸다.
- [ ] S5. 조각이 죽으면 화면이 침묵하지 않는다 — `?oops` 상자가 어느 파일이 왜 죽었는지와
      그 브라우저가 무엇을 할 줄 아는지를 함께 낸다. **실물로 확인했다**(첨부 캡처):
      「화면 조각은 모두 살아 있습니다」 + 지원 표 6항목 + UA. 알리는 조각만 ES5 로 썼다 —
      문법으로 죽은 것을 알리는 파일이 문법으로 죽으면 알릴 사람이 없다.
- [ ] S6. 크롬에서 문서 렌더가 그대로다 — `verify-doctable.html` 을 찍어 표(머리·정렬·
      여러 줄)·제목·굵게·인라인 코드·링크·목록이 모두 그려지는 것을 눈으로 봤다(첨부 캡처).

**여전히 사파리에서는 확인하지 못했다.** 위는 크롬 실측 + 사파리 문법 하한의 결합이지
"사파리에서 된다"의 증명이 아니다. 그 한 번은 친구분의 `?oops` 캡처가 답한다.

[Image: assets/REQ-20260829-038-62x6/s-038-table.png]

### 2026-09-06T00:03:18+09:00 response (agent: lead:claude-fable-5-1, by nicehugepark)

재점검 2026-09-06 (REQ-20260905-033-62x6 잔여 일괄): 이 라운드에도 맥이 없다 — 이 기계는 WSL, 원격 jade(eek-p620)는 Ubuntu 6.17. 맥 없이 더 고치는 것은 짐작이라(18327행 원칙) 코드를 만지지 않았다. 해제 조건은 그대로: 맥에서 `cd ~/section9 && ./bin/s9 doctor --live` 출력 한 장 + 사파리 `?oops` 캡처 한 장.

## History
- 2026-08-29T22:28:15+09:00 created by nicehugepark (status: open)
- 2026-08-29T22:28:31+09:00 status: open -> in-progress (by nicehugepark) — 037 에서 갈라 착수 — 사파리도 캡처도 없으므로 짐작이 아니라 사파리가 못 쓰는 문법을 코드에서 세는 것부터
- 2026-08-30T13:24:51+09:00 status: in-progress -> review (by nicehugepark) — 사파리가 문법으로 못 읽던 한 줄을 고쳤습니다 — 문서의 표를 그리는 정규식이었고, 그 한 줄이 죽으면 문서 본문이 통째로 안 그려집니다. 친구분께 주소 끝에 ?oops 를 붙여 열고 맨 위 상자를 캡처해 보내 달라고 해 주세요. 문서가 제대로 보인다고 하면 승인, 여전히 깨지면 그 캡처를 붙여 반려해 주세요.
- 2026-08-31T16:17:35+09:00 status: review -> in-progress (by nicehugepark) — 경유 전이(REQ-20260831-017-62x6 정리) — blocked 로 옮기기 위한 중간 단계
- 2026-08-31T16:17:35+09:00 status: in-progress -> blocked (by nicehugepark) — 사용자 행동 대기: 이 기계에 사파리·맥이 없어 리드가 직접 검증할 수 없다 — 승인/반려로 답할 자리가 아니라 blocked 가 맞다(037 과 같은 계열). 해제 조건: 맥에서 주소 끝 ?oops 를 붙여 열어 맨 위 상자 캡처 확보 — 문서가 제대로 보이면 done, 깨지면 그 캡처로 재작업
