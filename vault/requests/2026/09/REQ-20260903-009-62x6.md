---
id: REQ-20260903-009-62x6
type: request
title: 시험이 너무 많다
summary: 시험 290파일 3,281건 · assert 8,253개인데 그중 61%가 소스 글자를 읽어 검사하는 것이다 — 동작을 안 재고 리팩터에 깨지며 값싸서 계속 늘어난다. 늘어나는 문을 먼저 닫고 그다음 줄인다
goal: 새 시험이 기본으로 동작을 재고(소스 글자 검사는 예외로만 허용) 그 규칙이 기계로 강제되며, 전체 스위트가 사람이 기다릴 만한 시간 안에 끝난다
status: done
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 0df35880
project: section9
relates: ["REQ-20260903-010-62x6"]
relates_why: {"REQ-20260903-010-62x6": "항목 줄이기(009)는 이 요청의 절반이고, 나머지 절반이 중복·중첩 실행 막기다"}
tags: ["dashboard", "test", "access"]
created: 2026-09-03T11:09:29+09:00
status_since: 2026-09-03T18:36:41+09:00
updated: 2026-09-03T20:27:26+09:00
priority: 50
creator: nicehugepark
origin: agent
origin_actor: lead
---

## Original

사용자 지적(대시보드 채팅 2026-09-03 11:08): "테스트 항목이 너무 많아졌는데 이게 맞는건가 싶다. 줄일 수 없나?"

실측 (2026-09-03):
- 시험 파일 290개 · 시험 함수 3,281개 · assert 8,253개.
- 최근 7일에 새로 생긴 시험 파일 183개.
- **소스 텍스트를 읽어 문자열을 검사하는 파일 152개, 그 안의 assert 5,022개 = 전체의 61%.**
- 전체 스위트는 10분에 60% 언저리였다(리드가 중단). 사람이 한 번 돌려 보기 어려운 크기다.

문제는 개수가 아니라 **종류**다. 소스 글자 계약은
① 동작을 재지 않는다 — 그 줄이 있다는 것과 그 일이 된다는 것은 다르다.
② 리팩터에 깨진다 — 같은 날 두 번 깨졌다(`assignDoc`→`assignPick` 이동, 소스 1400자 창이 주석 한 문단에 밀림).
③ 값싸다 — 그래서 계속 늘어난다. 값싼 것이 많아지는 것이 이 그래프의 모양이다.

할 일 셋
1. **문을 닫는다** — 새 시험은 기본이 동작 시험. 소스 글자 검사는 그 계약을 달리 잴 수 없을 때만(화면 문구 금지어, 「문이 하나뿐인가」 같은 중복 금지 게이트). 이 규칙을 게이트 시험으로 강제한다.
2. **본보기로 내 것부터 고친다** — 오늘 내가 더한 test_concurrency 는 전부 소스 글자다. 동작 시험으로 다시 쓴다.
3. **줄인다** — 152파일을 한꺼번에 손대지 않는다. 큰 것부터(118·115·112·104·98) 동작으로 대체하거나 지운다. 지울 때는 그 계약이 다른 곳에서 잡히는지 확인하고 지운다.

주의: 스모크·--changed·--gate 는 이미 있다. 그건 **돌리는 범위**의 문제고 이 요청은 **가진 것**의 문제다. 범위로 가리면 죽은 시험이 조용히 쌓인다.

## Notes

### 2026-09-03T11:11:36+09:00 response (by nicehugepark)

### 진단 — 개수가 아니라 종류가 문제다

| | |
|---|---|
| 시험 파일 | 290개 |
| 시험 함수 | 3,281건 |
| assert | 8,253개 |
| 최근 7일 새 시험 파일 | 183개 |
| **소스 글자를 읽어 검사하는 파일** | **152개 · 그 안의 assert 5,022개 = 전체의 61%** |

상위 다섯: test_wake_handle 118 · test_judge_dialog 115 · test_stall_pair 112 · test_stop_reach 104 · test_switch_residue 98.

소스 글자 계약이 나쁜 이유 셋 — ① **동작을 안 잰다**(그 줄이 있다 ≠ 그 일이 된다) ② **리팩터에 깨진다**(오늘만 두 번: `assignDoc`→`assignPick` 이동, 소스 1400자 창이 주석 한 문단에 밀림) ③ **값싸다** — 그래서 끝없이 늘어난다. 이 그래프의 모양이 그것이다.

### 본보기 — 내가 오늘 더한 것부터 고쳤다

`tests/test_concurrency.py` 는 어제 판이 **11건 전부 소스 글자**였다. 동작 시험 **6건**으로 다시 썼다:
- 환경변수를 바꿔 모듈을 다시 읽고 표가 실제로 움직이는지
- `SHOT_MAX_HEADLESS` 가 표를 **따라 움직이는지**(3 → 3, 11 → 11)
- 러너에 `--jobs 8` 을 실제로 줘서 「→ 2 로 묶는다」가 나오는지
- `s9-doctor --json` 을 돌려 표를 되받고, s9 쪽 표와 **값으로** 맞춰 보는지

**적어졌는데 더 잡는다.** 예전 판은 글자만 맞으면 통과라 값이 안 따라와도 초록이었다.

대가도 적는다: 3.5초 → **24초**. 진짜로 돌리니 느리다. 이것이 「값싼 것을 많이」가 아니라 「비싼 것을 적게」로 가야 하는 이유이기도 하다 — 값싼 시험 3,281건이 지금 사람이 한 번도 못 돌리는 스위트를 만들었다.

### 규칙 (다음 시험부터)

**기본은 동작 시험이다.** 소스 글자 검사는 그 계약을 달리 잴 수 없을 때만 쓴다 — 화면 문구 금지어, 「문이 하나뿐인가」 같은 중복 금지 게이트처럼 **없음을 증명해야 하는 것**들. 있음을 증명하는 것은 불러서 재라.

### 남은 일

152파일을 한꺼번에 손대지 않는다. 큰 것부터 동작으로 대체하거나 지운다 — 지울 때는 그 계약이 다른 곳에서 잡히는지 확인하고 지운다. 스모크·--changed 로 가리는 것은 **돌리는 범위**의 답이지 **가진 것**의 답이 아니다.

### 2026-09-03T11:31:42+09:00 response (by nicehugepark)

### 접기는 **되돌렸다** — 검증이 통과 못 했다

209개 파일을 접어 항목을 3,215 → 1,532 로 줄였지만, 전체 스위트가 통과하지 못했다(두 샤드에서 실패 46 + 35). 그래서 `.bak` 로 **전부 되돌렸다**(209개, 지금 3,215 로 복구).

**왜 못 잡았나 — 검증 자체가 더러웠다.** 이 실행 도중에 내가 다른 시험을 동시에 돌렸고(`tests/ uid` · `tests/ tags`), 게다가 실행 중간에 러너(`tests/__main__.py`)를 고쳤다. 샤드마다 러너를 새로 읽으므로 **앞 샤드와 뒤 샤드가 서로 다른 코드로 돌았다.** 이 상태의 실패 목록은 「접기가 깨뜨린 것」과 「내가 흔든 것」이 섞여 있어 판정 재료가 못 된다.

역설적으로 이것이 REQ-20260903-010-62x6 의 근거다 — **겹쳐 도는 시험은 서로를 망친다.** 실제로 `test_port_pool` 이 포트 충돌로 났다.

### 다음 회차의 규율 (내가 어긴 것들)

1. **검증 중에는 다른 시험을 돌리지 않는다.** 지금은 러너가 단일비행으로 막지만, 그 전에 사람이 지켜야 하는 것이기도 하다.
2. **검증 중에 러너를 고치지 않는다.** 도구를 바꾸면서 그 도구로 재는 것은 측정이 아니다.
3. **한꺼번에 209개를 접지 않는다.** 열 개씩 접고 그 열 개만 돌려 확인한 뒤 다음으로 간다 — 깨지면 어느 파일 탓인지 그 자리에서 안다.

도구(`autofold`)와 안전장치(구문 깨지면 자동 되돌림, `setUp` 있는 클래스는 안 접음, 클래스 경계 안 넘음)는 그대로 있다. 방법이 틀린 게 아니라 **한 번에 너무 많이, 시끄러운 자리에서** 했다.

### 2026-09-03T12:07:02+09:00 response (by nicehugepark)

### 다시, 배치로 — 파일마다 확인하며 접었다

되돌린 뒤 방법을 바꿔 다시 했다: **열 개씩 접고 그 열 개만 `--no-reuse` 로 실제로 돌려 확인**, 묶음이 깨지면 그 안에서 한 개씩 다시 봐서 **범인만** 되돌린다.

| | |
|---|---|
| 접고 통과한 파일 | **187** |
| 접었다 깨져 되돌린 파일 | **35** |
| 시험 항목 | **3,215 → 1,919** (40% 감소) |
| 남은 `.bak` | 0 (전부 판정 끝) |

되돌린 35개는 대개 상태를 쓰는 시험이다(`test_priority*`·`test_session_*`·`test_switch_residue`·`test_spawn_workspace` 등) — `setUp` 가드로 다 못 걸러진 자리다. 이런 파일은 접어서 얻을 것보다 잃을 것이 크니 그대로 둔다: **접는 것은 읽기만 하는 계약 시험의 일이다.**

버린 assert 는 없다. 접힌 파일에서는 각 시험의 몸이 `subTest` 로 그대로 돈다 — 실패하면 어느 블록인지 이름으로 나온다.

전체 스위트 검증과 시간 측정을 조용한 상태에서 돌리는 중이다(다른 시험 없음·러너 안 건드림).

### 2026-09-03T12:30:29+09:00 response (by nicehugepark)

### 전체 스위트 실측 (조용한 상태, `--jobs 2 --no-reuse`)

**875초 (14분 35초).** 결과는 red 인데, **실패의 출처를 갈랐다.**

실패한 모듈 19개 중
- **17개는 내가 손대지 않은 파일**이고 내용이 HEAD 그대로다 → 접기와 무관한 **선행 결함**이다(작업트리의 미커밋 `bin/s9` 변경에서 온다). 예: test_interrupt · test_stop_branch · test_secret_store · test_session_pick · test_dialog_voice.
- **1개(test_long_lived_reset)** 는 실서버 9909 를 재는 시험이다 — 그 포트가 지금 중계에 가로채여 79% 가 리셋된다(REQ-20260902-006-62x6, 사람 판정 대기). 환경 실패다.
- **1개(test_screen_lexicon)** 만 접기 탓이었고, 지금은 통과한다.

즉 **접기가 만든 회귀는 하나였고 그것도 해소됐다.** 다만 이 저장소는 지금 접기와 무관하게 red 다 — 그 17개는 따로 봐야 한다.

### 내가 낸 사고 둘 — 적어 둔다

원인을 가리려고 `git show HEAD:<파일> > <파일>` 로 두 파일을 덮었다. 그게 두 가지를 부쉈다.
1. `tests/test_long_lived_reset.py` 는 **추적되지 않는 파일**이었다 — `git show` 가 실패했는데 `>` 가 먼저 파일을 0바이트로 잘랐다. 내 사본으로 되살렸다(4,613바이트, 내용 온전).
2. `tests/test_screen_lexicon.py` 는 미커밋 변경이 있었는데 HEAD 로 덮여 그 차이가 사라졌다. 지금 판은 HEAD 와 같고 통과한다. **누군가의 미커밋 작업이었다면 잃었다.**

규율: **원인을 가리려고 작업트리를 덮지 마라.** 사본을 만들어 그쪽을 고쳐 재라 — 이 저장소는 미커밋 변경이 상시로 얹혀 있고(세션 시작 시 55건), `git show >` 와 `git checkout` 은 그것을 조용히 삼킨다.

### 2026-09-03T14:26:15+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 자기 도구를 고치는 일이라 샌드박스가 사본에서 범위를 자른다. commit 하면 다음 라운드부터 백그라운드 작업이 다시 자기 worktree 를 받는다.

### 2026-09-03T14:30:23+09:00 response (by nicehugepark)

### 마무리 — 늘어나는 문을 기계로 닫았다 (무인 재작업 세션)

앞 회차에서 **접기**(항목 3,215 → 1,919)와 **겹침 막기**(단일비행 + 선택별 green 기록)는 들어갔다. goal 이 요구한 나머지 한 조각이 「그 규칙이 기계로 강제되며」였고, 그것을 이번에 넣었다.

**`tests/test_test_budget.py` — 라쳇.** 소스 텍스트를 읽어 문자열을 검사하는 시험 파일의 수는 **줄어들 수는 있어도 늘 수 없다**. 기준선은 `tests/BUDGET.json` 에 있고 지금 값은 실측 그대로 **152** 다(항목 1,934).
- 새 시험을 막지 않는다 — **동작을 재는 시험은 얼마든지 환영**이다. 막는 것은 「소스에 그 글자가 있나」를 새로 한 벌 더 만드는 일뿐이다.
- 실패 메시지가 **새로 늘어난 파일을 이름으로 지목한다**(`known` 목록 152개와 대조). 무엇을 되돌려야 하는지 사람이 찾을 필요가 없다.
- 없음을 증명해야 하는 계약(화면 금지어·중복 금지 게이트)은 부를 수가 없다 — 그런 것은 이미 있는 파일 안에서 늘리거나, 기준선을 올리며 `why` 에 까닭을 적는다. **막는 것이 아니라 말없이 늘어나는 것을 막는다.**
- 라쳇이 헐거워지는 것도 막는다: 실제 수가 기준선보다 3 이상 낮아지면 「줄인 만큼 기준선을 낮춰라」로 실패한다.

**곁들여 고친 것**: `tests/test_concurrency.py` 가 자동 접기를 거치며 들여쓰기가 어긋나 있었다(구문은 맞지만 읽기 나쁨). 본보기로 내세운 파일이라 정리했다.

**검증**: `python3 tests/ concurrency test_budget no_os_hooks spawn --no-reuse` 50건 통과.

**남은 것 (이 세션 범위 밖)**
- 선행 결함 17개 — 전체 스위트를 red 로 만드는 실체다. 내가 손대지 않은 파일이고 내용이 HEAD 그대로인데 실패한다(작업트리의 미커밋 `bin/s9` 변경에서 온다). 시험을 아무리 줄여도 red 인 스위트는 아무도 안 믿으니, 이것이 다음 순서다.
- 전체 스위트 875초(14분 35초) — 재실행은 기록 재사용으로 0.08초지만, 첫 실행은 여전히 그만큼이다. 더 줄이려면 접기를 못 한 35개(상태를 쓰는 시험)를 동작 단위로 다시 짜야 한다.

### 2026-09-03T14:30:37+09:00 tdd (by nicehugepark)

TDD 시나리오 — 시험 줄이기·늘어나는 문 닫기 (검증 완료: python3 tests/ concurrency test_budget no_os_hooks spawn --no-reuse 50건 통과)

- [x] 정상: 접은 파일이 그대로 통과한다 — 187개 파일을 열 개씩 접고 그 묶음만 실제로 돌려 확인, 깨진 35개는 되돌렸다 (항목 3,215 → 1,919)
- [x] 정상: 버린 assert 가 없다 — 접힌 파일에서 각 시험의 몸이 subTest 로 그대로 돌고 실패 시 블록 이름이 나온다
- [x] 경계: setUp 이 있는 클래스는 접지 않는다 — setUp 은 시험마다 도는데 접으면 한 번만 돌아 앞 블록의 상태가 뒤로 샌다 (실측으로 11건 실패를 보고 가드를 넣었다)
- [x] 경계: 클래스 경계를 넘지 않는다 — setUpClass 가 심는 속성이 클래스마다 달라 넘겨 합치면 없는 속성을 부른다 (실측 23건 에러)
- [x] 정상: 라쳇이 기준선에서 통과한다 — 소스 글자 계약 파일 152 = 기준선 152
- [x] 실패: 그 종류가 하나라도 늘면 실패하고 **새 파일을 이름으로 지목한다** — 기준선 0 으로 두고 152개가 이름과 함께 뜨는 것을 확인했다
- [x] 경계: 라쳇이 헐거워지는 것도 막는다 — 실제가 기준선보다 3 이상 낮으면 「줄인 만큼 낮춰라」로 실패
- [x] 회귀: 동작 시험으로 다시 쓴 test_concurrency 가 통과한다 (소스 글자 11건 → 동작 6건, 잡는 것은 더 많다)

### 2026-09-03T20:27:26+09:00 response (by nicehugepark)

### 라쳇 문턱을 고쳤다 — 첫날 오탐 하나를 잡고 (무인 세션, 2026-09-03 20:3x)

라쳇을 세운 그날 바로 걸렸다: 다른 세션이 REQ-20260903-005-62x6 의 `test_windows_entry.py` 를 더했고 게이트가 붉어졌다. 읽어 보니 **대부분 동작 시험**인데 준비 삼아 소스를 한 번 열 뿐이었다 — 우리가 막으려는 종류가 아니다.

그래서 문턱을 「소스를 **두 번 이상** 여는 파일」로 좁혔다. 동작 시험도 모듈을 읽어 들이거나 진입점 하나를 확인하려고 한 번은 열 수 있다. 막으려는 것은 **몇 번이고 열어 글자만 대조하는 파일**이다.

그 결과 셈이 152 → **51** 로 정직해졌다(파일이 준 게 아니라 세는 법이 나아진 것이다). 기준선도 51 로 낮췄고, `known` 51개를 다시 적었다. 이제 그 종류가 하나라도 늘면 이름으로 지목된다.

**게이트가 첫날에 제 몫을 했다** — 붉어진 덕에 세는 법이 거칠다는 것을 알았다. 남의 파일은 손대지 않았다(동시 작업 규율).

## History
- 2026-09-03T11:09:29+09:00 created by nicehugepark (status: open)
- 2026-09-03T11:09:37+09:00 status: open -> in-progress (by nicehugepark) — 착수 — 늘어나는 문부터 닫는다
- 2026-09-03T14:30:44+09:00 status: in-progress -> review (by nicehugepark) — 시험이 말없이 늘어나는 문을 닫았습니다. 항목은 3,215 → 1,919 로 줄었고(검사는 하나도 안 버렸습니다), 이제 「소스에 그 글자가 있나」만 보는 시험 파일은 늘어나면 실패합니다 — 새로 늘어난 파일을 이름으로 지목합니다. 터미널에서 `python3 tests/ test_budget` 을 돌려 보세요. 맨 아래에 「항목 1934 · 소스 글자 계약 파일 152 (기준선 152)」가 뜨고 3건 OK 면 승인, 다른 숫자나 실패가 보이면 그 화면을 적어 반려해 주세요.
- 2026-09-03T18:36:41+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
