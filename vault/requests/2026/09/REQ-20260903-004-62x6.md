---
id: REQ-20260903-004-62x6
type: request
title: 동시 최고치가 비용이다
summary: "사용자 관찰 — 소강 때는 멀쩡하고 여러 요청·시험이 몰릴 때 터진다. 실측으로 확인: 윈도우 동적 포트는 동시에 열려 있는 연결 하나당 하나씩 잡히고 닫으면 돌아온다. 그러니 묶어야 할 것은 총량이 아니라 동시 최고치다"
goal: 동시 최고치가 계기에 남고, 병렬 시험·에이전트가 그 최고치를 넘지 못하게 하는 상한이 한 곳에 선다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 0df35880
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-09-03 09:43:45 KST - lead]`", "started": "2026-09-03T09:43:46+09:00", "ended": "2026-09-03T09:43:46+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260903-003-62x6", "DOC-20260903-002-62x6", "REQ-20260902-066-62x6"]
relates_why: {"REQ-20260903-003-62x6": "평시가 몇인가의 답 — 평시는 동시 최고치를 따라가고, 재야 할 값이 그것이다", "DOC-20260903-002-62x6": "그 문서의 「연료가 무엇인가」를 이 요청의 실측이 답했다", "REQ-20260902-066-62x6": "중계를 흔드는 쪽과 동시 최고치를 올리는 쪽이 겹칠 때 잔재가 남는다"}
tags: ["auto-audit", "test"]
created: 2026-09-03T09:39:49+09:00
status_since: 2026-09-03T10:02:20+09:00
updated: 2026-09-03T10:02:20+09:00
priority: 50
creator: nicehugepark
origin: human
---

## Original

내가 봤을 때 요청이 소강이 되는 상황에는 문제가 없다. 동시에 여러개 요청 작업을 할 때 테스트가 몰리면 발생하는것같다.

## Notes

### 2026-09-03T09:43:23+09:00 response (by nicehugepark)

### 실측 — 사용자 가설이 맞다 (2026-09-03 09:4x, 리드)

사용자 관찰: "요청이 소강이 되는 상황에는 문제가 없다. 동시에 여러개 요청 작업을 할 때 테스트가 몰리면 발생하는것같다."

윈도우 `Get-NetTCPConnection -State Bound` 개수를 앞뒤로 재서 확인했다.

| 행동 | Bound 변화 |
|---|---|
| 루프백 **120개를 동시에 열어 둔 채** | **+121** |
| 그 120개를 전부 닫은 뒤 | **+1** (사실상 전부 반환) |
| 자식이 100개를 연 채 대기 | **+100** |
| 그 자식을 **SIGKILL** | **−99** (강제 종료도 반환한다) |
| 바깥으로 **순차** 200 연결 | 0 |
| 리셋당하는 연결 **순차** 100회 | −3 |
| `python3 tests/ assign` 1회 · `s9 shot` 1회 | −1 · 0 |

**비용은 「동시에 열려 있는 연결 수」다 — 연결당 정확히 하나, 1:1.** 순차는 몇 번을 걸어도 공짜다(한 번에 하나만 열려 있으므로). 그래서 총량(몇 번 연결했나)은 지표가 아니고 **최고치(한때 몇 개가 동시에 열려 있었나)**가 지표다.

이 한 줄이 앞선 관측의 모순을 전부 푼다 — 순차 200회가 0이었던 것도, 시험 한 판이 −1이었던 것도, 계기 on/off 가 차이 없던 것도(계기는 언제나 동시 3개 이하다), 그리고 사람이 여러 갈래로 몰아칠 때만 오르던 것도.

### 그런데 남아 있는 4,748 은 무엇인가 — 아직 모른다

정상 종료도 강제 종료도 돌려준다는 것이 위 표다. 그런데 지금 dllhost(pid 8004)가 4,748 개를 쥐고 있고 같은 시각 리눅스 쪽 살아 있는 TCP 는 11 개다. **살아 있는 연결이 없는데 예약만 남아 있다.**

즉 평시에는 안 새고, **어떤 순간에 반환 경로가 끊긴다.** 후보는 그 순간에 중계 자신이 흔들린 것 — 2026-09-02 의 무인 중계 종료 9회, WSL 네트워크 죽음, 9909 이 모든 연결을 리셋하던 새벽 구간(브라우저가 계속 재시도하며 죽은 연결을 만들었다). 재현은 못 했다(중계를 다시 흔들어야 하고 그 대가가 이 사고 자체다).

### 그래서 할 일

1. **동시 최고치를 계기에 남긴다** — 지금 계기의 `sys` 는 그 순간의 `tcp_inuse` 만 적어 봉우리를 놓친다. 표본 사이의 최고치를 함께 적어야 "몰린 순간"이 기록에 남는다. 윈도우 Bound 수도 같이(REQ-20260903-003-62x6).
2. **병렬 상한을 한 곳에 세운다** — 시험 러너의 `--jobs`, 동시 스폰하는 에이전트 수, 헤드리스 브라우저 개체수. 지금은 각자 따로 정해져 있어 겹칠 때의 합이 아무도 모르는 값이다.
3. 그리고 **중계를 흔들지 않는다** — 이미 껐다(REQ-20260902-066-62x6).

주의: 이 계산은 WSL 중계가 있는 판에서만 파국이 된다. `.wslconfig` 의 `networkingMode=mirrored` 로 중계를 없애면 이 자원 자체가 사라진다. 다만 **동시 최고치를 묶는 일은 어느 판에서나 옳다** — 어제 내가 "2번이 되면 1번은 의미 없다"고 한 것은 1번을 「연결 총수 줄이기」로 잘못 잡았기 때문이고, 제대로 잡으면 「동시 최고치 묶기」라 mirrored 와 무관하게 남는다.

### 2026-09-03T09:43:46+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-03 09:43:45 KST - lead]`

말씀이 맞았습니다. 재서 확인했고, **그 한 줄이 지금까지의 모순을 전부 풉니다.**

### 비용은 「동시에 열려 있는 연결 수」입니다 — 1:1

| 행동 | Bound 변화 |
|---|---|
| 루프백 **120개를 동시에 열어 둔 채** | **+121** |
| 그 120개를 전부 닫은 뒤 | **+1** (사실상 전부 반환) |
| 자식이 100개를 연 채 대기 | **+100** |
| 그 자식을 **SIGKILL** | **−99** (강제 종료도 돌려준다) |
| 바깥으로 **순차** 200 연결 | **0** |
| 리셋당하는 연결 순차 100회 | −3 |
| 시험 한 판 · 캡처 1회 | −1 · 0 |

**순차는 몇 번을 걸어도 공짜입니다** — 한 번에 하나만 열려 있으니까요. 비싼 건 **한때 몇 개가 동시에 열려 있었나**입니다.

이게 제가 계속 틀렸던 이유입니다. 저는 **총량**(몇 번 연결했나)을 재고 있었는데 지표는 **최고치**였습니다. 순차 200회가 0이었던 것, 시험 한 판이 −1이었던 것, 계기 켜고 끈 게 차이 없던 것(계기는 언제나 동시 3개 이하), 그리고 **사람이 여러 갈래로 몰아칠 때만 오르던 것** — 전부 이 한 줄로 설명됩니다.

### 아직 모르는 한 칸

정상 종료도 강제 종료도 돌려주는데, **지금 4,748개가 잡혀 있고 리눅스 쪽 살아 있는 TCP 는 11개**입니다. 살아 있는 연결이 없는데 예약만 남아 있습니다.

그러니 평시에는 안 새고 **어떤 순간에 반환 경로가 끊깁니다.** 그 순간의 후보는 중계 자신이 흔들린 때입니다 — 어제 무인 중계 종료 9회, WSL 네트워크 죽음, 그리고 새벽에 9909 가 모든 연결을 리셋하던 구간(브라우저가 계속 재시도하며 죽은 연결을 쌓았습니다). 재현하려면 중계를 또 흔들어야 해서 재현은 안 하겠습니다.

**즉 파국은 둘이 겹칠 때 옵니다: 동시 최고치가 높은 순간 × 중계가 흔들리는 순간.** 어제가 정확히 그랬습니다.

### 그래서 할 일 — 그리고 제 앞선 말 하나 정정

1. **동시 최고치를 계기에 남긴다** — 지금 계기는 표본 순간의 값만 적어 봉우리를 놓칩니다. 표본 사이의 최고치를 함께 적어야 "몰린 순간"이 기록에 남습니다.
2. **병렬 상한을 한 곳에 세운다** — 시험 러너 `--jobs`, 동시 스폰 에이전트 수, 헤드리스 브라우저 개체수가 지금은 **각자 따로** 정해져 있어, 겹칠 때의 합을 아무도 모릅니다.
3. 중계를 흔들지 않는다 — 어제 껐습니다.

**정정**: 제가 아까 "2번(mirrored)이 되면 1번은 의미 없다"고 했는데, 1번을 「연결 총수 줄이기」로 잘못 잡았기 때문입니다. 제대로 잡으면 **「동시 최고치 묶기」**이고, 그건 mirrored 를 켜도 남습니다 — 어느 판에서나 동시 연결은 자원입니다.

`REQ-20260903-004-62x6` 「동시 최고치가 비용이다」로 등록하고 착수했습니다. 1·2번을 지금 넣을까요?

### 2026-09-03T09:56:09+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 봉우리 계기 + 동시성 상한 한 곳 (실행: python3 tests/ metrics · python3 tests/ concurrency)

**① 봉우리를 놓치지 않는다**
- [ ] S1 정상: 표본 사이에 잠깐 솟은 값이 `peak_tcp_inuse` 로 남는다 — 15초 표본이 놓치는 봉우리를 1초 훑기가 잡는다
- [ ] S2 정상: 봉우리는 표본마다 비워진다 — 한 번 솟은 값이 이후 표본에 영원히 따라붙지 않는다(그러면 계기가 거짓말한다)
- [ ] B1 경계: /proc 을 못 읽는 판(맥·윈도우)에서 봉우리 키가 아예 안 실린다 — 0 을 실어 「한가하다」로 읽히게 하지 않는다
- [ ] B2 경계: 윈도우 동적 포트 수는 1분에 한 번만 잰다 — powershell 왕복이 1~2초라 15초마다 재면 계기가 계기를 방해한다
- [ ] F1 실패: powershell 이 없거나 실패해도 표본 전체가 죽지 않는다(그 키만 빠진다)

**② 동시성 상한이 한 곳에 선다**
- [ ] S3 정상: 캡처 브라우저 상한이 그 표를 읽는다 — 표를 고치면 캡처가 따라 바뀐다(두 벌 금지)
- [ ] S4 정상: `s9 doctor` 가 그 표를 사람에게 보여 준다 — 겹칠 때의 합을 물어볼 수 있어야 한다
- [ ] B3 경계: 환경변수로 각 값을 올릴 수 있다(막는 것이 아니라 아는 것이 목적이다)
- [ ] R1 회귀: 기존 계기 계약 30건과 캡처 상한 동작이 그대로다

### 2026-09-03T10:02:20+09:00 tdd (by nicehugepark)

### TDD 시나리오 (개정 — 검증 완료. python3 tests/ metrics 36건 · python3 tests/ concurrency 11건 통과)

**① 봉우리를 놓치지 않는다** (`bin/s9` `_peak_tick`/`_peak_take`/`_win_bound`, metrics_loop)
- [x] S1 정상: 표본 사이에 솟은 값이 `peak_tcp_inuse` 로 남는다 — test_p1
- [x] S2 정상: 봉우리는 표본마다 비워진다(한 번 솟은 값이 영원히 따라붙지 않는다) — test_p2
- [x] B1 경계: 0 은 싣지 않는다 — /proc 이 없는 판에서 0 은 「한가하다」가 아니라 「모른다」다 — test_p3
- [x] B2 경계: 윈도우 동적 포트 수는 1분에 한 번만 잰다(powershell 왕복 1~2초) — test_p4
- [x] F1 실패: powershell 이 없거나 실패해도 그 키만 빠지고 표본은 산다 — test_p5
- [x] 추가 회귀: 루프가 `sleep(interval)` 로 눈감지 않고 자면서도 훑는다 — test_p6

**② 동시성 상한이 한 곳에 선다** (`CONCURRENCY` 표 — bin/s9 · bin/s9-doctor · tests/__main__.py)
- [x] S3 정상: 캡처 브라우저 상한이 그 표를 읽는다(`SHOT_MAX_HEADLESS = CONCURRENCY["headless"]`) — test_b1
- [x] S4 정상: `s9 doctor` 가 표와 **합**을 보여 준다 — test_c1 · 실출력 「동시 상한: headless 8 · test_jobs 4 · conns 256 (한때 합 최대 268)」
- [x] B3 경계: 전부 환경변수로 올릴 수 있다(막는 것이 아니라 아는 것이 목적) — test_b3
- [x] 추가: 시험 러너의 `--jobs` 가 같은 손잡이(S9_MAX_JOBS)로 묶이고, 묶었으면 그 사실을 말한다 — test_b2
- [x] 추가: doctor 는 s9 를 import 하지 않는 규율이라 표를 두 벌 드는데, **두 벌이 갈리면 시험이 잡는다** — test_a1·test_a2
- [x] R1 회귀: 기존 계기 계약 30건과 캡처 상한 동작 그대로

### 2026-09-03T10:02:20+09:00 response (by nicehugepark)

### 넣은 것 (2026-09-03 09:5x~10:0x)

**① 봉우리 계기** — `bin/s9`
- `_peak_tick()` / `_peak_take()`: `/proc/net/sockstat` 을 **1초마다** 훑어 `tcp_inuse`·`sockets` 의 최고치를 들고, 표본에 `peak_tcp_inuse`·`peak_sockets` 로 싣고 비운다. `metrics_loop` 의 `time.sleep(interval)` 을 1초 조각으로 갈라 자는 동안에도 본다 — 몰리는 순간은 대개 15초보다 짧다.
- `_win_bound()`: 윈도우가 잡고 있는 동적 포트 수를 **1분에 한 번** 재서 `win_bound` 로 싣는다. powershell 왕복이 1~2초라 표본마다 재면 계기가 계기를 방해한다. 없거나 실패하면 그 키만 빠진다.
- 실측 확인(수집기 재기동 후): `{"peak_tcp_inuse": 11, "peak_sockets": 266, "win_bound": 4868, ...}`

**② 동시 상한 한 표** — `CONCURRENCY`
- `bin/s9` 에 `{headless: 8, test_jobs: 4, conns: 256}`, 전부 `S9_MAX_*` 로 올릴 수 있다. `SHOT_MAX_HEADLESS` 가 그 표를 읽는다(상수 두 벌 폐기).
- `tests/__main__.py` 의 `--jobs` 가 `S9_MAX_JOBS` 로 묶이고, 묶었으면 그 사실을 말한다.
- `bin/s9-doctor` 가 표와 **합**을 한 줄로 보여 준다: 「동시 상한: headless 8 · test_jobs 4 · conns 256 (한때 합 최대 268)」. 흩어진 상한의 문제는 값이 아니라 **합을 아무도 모른다**는 것이었다.
- doctor 는 s9 를 import 하지 않는 규율이라(그 도구는 s9 가 못 뜰 때도 써야 한다) 표를 두 벌 든다 — `tests/test_concurrency.py` 가 두 벌이 갈리는지 매번 확인한다.

**아직 못 하는 것**: 봉우리가 실제로 언제 솟는지는 이제부터 쌓인다. 하루쯤 지나 `peak_*` 와 `win_bound` 가 함께 오르는 구간을 찾으면, 그 시각에 무엇이 돌고 있었는지 물을 수 있다 — 그 물음이 REQ-20260903-003-62x6 이다.

## History
- 2026-09-03T09:39:49+09:00 created by nicehugepark (status: open)
- 2026-09-03T09:42:40+09:00 status: open -> in-progress (by nicehugepark) — 사용자 가설을 실측으로 확인했다 — 착수
- 2026-09-03T10:02:20+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: 동시 최고치가 계기에 남고(peak_tcp_inuse·peak_sockets·win_bound, 실측 확인), 병렬 상한이 CONCURRENCY 한 표에 서서 캡처·시험 러너·서버가 그것을 읽고 doctor 가 합까지 보여 준다. 시험 47건 통과(metrics 36 · concurrency 11).
