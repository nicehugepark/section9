---
id: REQ-20260902-015-62x6
type: request
title: 두 머신 위장 시험 픽스처
summary: P0. 한 머신에서 S9_MACHINE 둘 + 공용 bare origin 으로 sync 왕복하는 tests/ 픽스처. 남의 반려 REQ 가 pull 되면 훅 주입·s9 next·워처 중 하나가 집는 현행 결함을 빨간 테스트로 고정한다 (deep-diver 재현 스크립트가 씨앗)
goal: S9_MACHINE=A/B 두 S9_ROOT 가 bare origin 으로 왕복하는 픽스처가 tests/ 에 있고 다른 테스트가 import 해 쓰며, 남의 반려 REQ 를 B 의 reopened/next/워처가 집는 현행 결함이 실패하는 테스트로 고정돼 있다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 67e42231
agents: ["sub:quality-assurance:a089e36d", "sub:quality-assurance"]
contributions: [{"actor": "sub:quality-assurance:a089e36d", "item": "015 두 머신 위장 시험 픽스처", "started": "2026-09-02T15:26:06+09:00", "ended": "2026-09-02T15:41:58+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/67e42231-60e4-4bcc-b051-5484fe7ffbb9/tasks/a089e36d133a734ca.output"}, {"actor": "sub:quality-assurance", "item": "TDD 시나리오 (두 머신 위장 픽스처 · tests/twomachine.py + tests/test_two_machine.p", "started": "2026-09-02T15:29:50+09:00", "ended": "2026-09-02T15:29:50+09:00", "result": "done", "transcript": ""}, {"actor": "sub:quality-assurance", "item": "두 머신 위장 픽스처 — 구현·실측 (quality-assurance)", "started": "2026-09-02T15:35:27+09:00", "ended": "2026-09-02T15:35:27+09:00", "result": "done", "transcript": ""}, {"actor": "sub:quality-assurance", "item": "추가 실측 — 반복 실행 안정성. `python3 tests/ two_machine` 을 12회 반복: 10회 동일(8 tes", "started": "2026-09-02T15:41:31+09:00", "ended": "2026-09-02T15:41:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:quality-assurance:a089e36d", "item": "`[2026-09-02 15:53:10 KST - quality-assurance]`", "started": "2026-09-02T15:41:58+09:00", "ended": "2026-09-02T15:41:58+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/67e42231-60e4-4bcc-b051-5484fe7ffbb9/subagents/agent-a089e36d133a734ca.jsonl"}]
project: section9
parent: REQ-20260902-014-62x6
relates_why: {}
tags: ["sync", "test", "assets"]
created: 2026-09-02T14:06:34+09:00
status_since: 2026-09-02T15:45:37+09:00
updated: 2026-09-02T15:45:37+09:00
priority: 75
---

## Original

설계: DOC-20260902-001-62x6 §5 P0. 씨앗: projects/section9/assets/014-sync-design/repro_cross_machine.py·repro_reassign.py

## Notes

### 2026-09-02T15:29:50+09:00 tdd (agent: sub:quality-assurance, by nicehugepark)

### TDD 시나리오 (두 머신 위장 픽스처 · tests/twomachine.py + tests/test_two_machine.py)

픽스처 자체(통과해야 옳음):
- [x] S1. 정상 — 왕복: alpha 에서 만든 REQ 가 `sync(alpha)`→`pull(beta)` 뒤 beta 의 `s9 show` 로 읽히고, beta 의 전이가 `sync(beta)`→`pull(alpha)` 로 alpha 문서 History 에 돌아온다. 두 루트의 HEAD 가 bare origin 의 main 과 같다.
- [x] S2. 경계 — 실스폰 0: FakePopen 이 `claude` argv 만 잡아 기록하고 git 은 실제로 돈다(sync 가 진짜 commit→pull→push). 바인딩(state/sessions)이 상대 머신에 pull 돼 보인다.

현행 결함 고정(지금 코드에서 **빨강**이어야 옳음 · 어느 REQ 가 초록으로 만드나):
- [x] S3. 회귀(a) — alpha/alice 의 review 문서를 beta 대시보드에서 반려 → beta 워처 `rework_watch_tick(grace=0)` 스폰 0. (현행 통과 — machine 가드가 막는다. 016 뒤에도 0 이어야 한다)
- [x] S4. 실패(b) — beta 세션이 `s9 claim` 으로 이어받고 sync 된 뒤 alpha 워처 tick 이 스폰 0 이어야 한다. 현행: beta 클레임을 죽은 세션으로 보고 1 스폰(repro_reassign [1]). → 016 게이트 단일화 / 020 리스.
- [x] S5. 실패(c) — bob@beta 의 `s9 reopened` 에 alice 의 반려 문서가 없어야 하고, alice@beta 의 `s9 next --json` 이 그 문서를 주지 않아야 한다. 현행: 둘 다 오른다(deep-diver [b]). → 016 (잠정 machine 키) / 020 (리스).
- [x] S6. 실패(d) — beta 에서 X 를 done 으로 닫아도 `trigger_dependents(X)` 가 alpha 의 blocked Y 를 beta 에서 재개하지 않아야 한다. 현행: freed 에 Y 가 들고 Y 가 in-progress 로 바뀐다(deep-diver [e]). → 016.
- [x] S7. 실패(e) — alpha 바인딩의 attach_pid 가 beta 의 산 pid 와 겹쳐도 beta 의 `chat_target()` 은 그 바인딩을 고르지 않는다. 현행: alpha/aaaa1111 을 고른다(deep-diver [f]). → 017 바인딩 불가침.

실행: `python3 tests/ two_machine` (기대: S1·S2·S3 통과, S4~S7 실패 = exit 1). 회귀: `python3 tests/ switch_residue auto_resume claim` exit 0.

### 2026-09-02T15:35:27+09:00 response (agent: sub:quality-assurance, by nicehugepark)

### 두 머신 위장 픽스처 — 구현·실측 (quality-assurance)

**파일 (tests/ 만, bin/s9 무수정)**
- `tests/twomachine.py` — 재사용 픽스처. 임시 디렉토리 아래 bare origin + S9_ROOT 둘(alpha/beta). alpha 는 git init → 인스턴스 .gitignore(INSTANCE_TRACK_GITIGNORE) + `.s9-sync` remote 마커 → `s9 init` → push; beta 는 clone → `s9 init`. 헬퍼: `cli(machine,*argv,sess=,user=)` · `load_mod(machine)`(모듈 이름 머신별, `current_machine` 고정) · `fake_spawn()`(Popen 을 바꿔 claude 는 삼키고 git 은 진짜로 — sync 가 그 안에서도 돌아야 하므로) · `tick(machine,grace)` · `sync(machine)`(`s9 sync` 가 'sync: ok' 아니면 실패로 올림) · `pull(machine)`(rebase+autostash 뒤 rebuild_index — 카탈로그는 track 안 되므로) · 씨앗 장면 `seed_alpha_review`·`reject_on`·`enable_worker`.
- `tests/test_two_machine.py` — 8 케이스, 클래스마다 새 픽스처(순서 의존 없음).

**실측** (`python3 tests/ two_machine`, 8 tests 20.0s, **exit=1** — 의도대로)
- 통과 3: S1 왕복(alpha 문서→beta show, beta 전이→alpha History, HEAD 셋 일치) · S2 바인딩 track + fake_spawn(git 진짜/claude 삼킴) · S3 (a) beta 워처 스폰 0 — 블록 사유 실측 `elsewhere`(machine 가드).
- **빨강 5 (현행 결함 고정)**: S4 (b) beta 클레임 뒤 alpha 워처가 1 스폰(claude argv 1건) → 016/020 · S5a (c) bob@beta `s9 reopened` 에 alice 문서 실림 → 016 · S5b (c) alice@beta `s9 next --json` 이 alpha 문서 권함 → 016/020 · S6 (d) beta 의 `trigger_dependents(X)` 가 alpha 의 blocked Y 를 재개(freed=[Y]) → 016 · S7 (e) alpha 바인딩 attach_pid 충돌 시 beta `chat_target()` 이 alpha/aaaa1111 선택 → 017.
- 회귀: `python3 tests/ switch_residue auto_resume claim` 91 tests **exit=0** · `runner_patterns tmp_hygiene stdlib_only` exit=0. 임시 자리 잔재 0(픽스처 close + tmproot).

**한계·메모**
- 016/020 뒤 S3 는 alpha 세션 A 의 리스가 신선하다는 전제로 0 이어야 한다 — 리스 TTL 을 바꾸면 이 씨앗(방금 전이)이 그 전제를 만족하는지 확인할 것.
- S6 의 기대는 「beta 가 Y 를 재개하지 않는다」까지만이다. blocked_by 목록 정리를 어느 머신이 해도 되는지는 016 이 정한다(시험은 그 부분을 단정하지 않음).
- 실행 시간의 대부분은 문서 이벤트마다 도는 실제 commit→pull→push 다(클래스당 ~4s). 더 줄이려면 씨앗을 공유하는 대신 격리를 잃으니 지금은 두었다.

### 2026-09-02T15:41:31+09:00 response (agent: sub:quality-assurance, by nicehugepark)

추가 실측 — 반복 실행 안정성. `python3 tests/ two_machine` 을 12회 반복: 10회 동일(8 tests · 3 ok · 5 FAIL · exit 1). 2회 setUpClass 오류가 났는데 둘 다 픽스처가 아니라 **동시 편집 중인 bin/s9** 가 원인이다 — 한 번은 s9 프로세스가 `SyntaxError: expected ':' (bin/s9:10329)` 로 죽었고(리드가 같은 시간에 bin/s9 를 고치는 중, git status M 157+/12-), 다른 한 번은 `s9 new` 가 rc 0 에 빈 출력을 냈다(같은 시각대의 중간 상태로 추정, 재현 불가). 픽스처는 그 경우를 삼키지 않도록 `new_request` 가 rc·stdout·stderr·vault 목록·sync.log 를 담아 실패로 올리게 했다(`fx.last` 에 마지막 CompletedProcess 보존). bin/s9 가 멈춘 트리에서 다시 돌리면 결정적이다.

### 2026-09-02T15:41:58+09:00 subagent (agent: sub:quality-assurance:a089e36d, by nicehugepark)

`[2026-09-02 15:53:10 KST - quality-assurance]`

### 결론

REQ-20260902-015-62x6 의 픽스처와 빨간 시험을 tests/ 에 넣었습니다. `python3 tests/ two_machine` 은 **8 tests · 3 ok · 5 FAIL · exit=1** — 의도대로입니다(픽스처 시험 S1·S2 와 회귀 고정 S3 통과, 현행 결함 S4~S7 빨강). bin/s9 는 읽기만 했고 수정하지 않았습니다. 상태 전이는 하지 않았습니다.

### 파일

- `/home/sjpark1/section9/tests/twomachine.py` — 재사용 픽스처. 임시 디렉토리 아래 bare `origin.git` + S9_ROOT 둘(alpha/beta). alpha: `git init` → 인스턴스 `.gitignore`(bin/s9 의 `INSTANCE_TRACK_GITIGNORE` 그대로) + `.s9-sync` remote 마커 → `s9 init` → push. beta: clone → `s9 init`. 헬퍼: `cli(machine, *argv, sess=, user=)` · `load_mod(machine)`(모듈 이름 머신별로 갈라 한 프로세스에 두 루트 공존, `current_machine` 고정) · `fake_spawn()`(Popen 을 바꾸되 `claude` 만 삼키고 `git` 은 진짜 Popen 으로 — sync 가 그 안에서도 실제로 돌아야 하므로) · `tick(machine, grace)` · `sync(machine)`(`sync: ok` 아니면 실패로 올림) · `pull(machine)`(rebase+autostash 뒤 `rebuild_index` — 카탈로그는 track 되지 않아 pull 만으로는 CLI 가 새 문서를 못 봄) · 씨앗 장면 `seed_alpha_review`·`reject_on`·`enable_worker`.
- `/home/sjpark1/section9/tests/test_two_machine.py` — 8 케이스, 클래스마다 새 픽스처(순서 의존 없음). 각 docstring 에 초록으로 만들 REQ 를 적었습니다.

### 실측 (`python3 tests/ two_machine`, 14.9s)

| 시나리오 | 결과 | 초록으로 만들 REQ |
|---|---|---|
| S1 왕복: alpha 문서→beta `show`, beta 전이→alpha History, HEAD 셋 일치 | ok | — |
| S2 바인딩 track + fake_spawn(git 진짜/claude 삼킴) | ok | — |
| S3 (a) beta 워처 스폰 0 — 블록 사유 실측 `elsewhere` | ok (회귀 고정) | 016·020 뒤에도 0 |
| S4 (b) beta `s9 claim` 뒤 alpha 워처가 1 스폰(claude argv 1건) | **FAIL** | 016 / 020 |
| S5a (c) bob@beta `s9 reopened` 에 alice 문서 실림 | **FAIL** | 016 |
| S5b (c) alice@beta `s9 next --json` 이 alpha 문서 권함 | **FAIL** | 016 / 020 |
| S6 (d) beta `trigger_dependents(X)` 가 alpha 의 blocked Y 재개(freed=[Y]) | **FAIL** | 016 |
| S7 (e) alpha 바인딩 attach_pid 충돌 시 beta `chat_target()` 이 alpha/aaaa1111 선택 | **FAIL** | 017 |

회귀: `python3 tests/ switch_residue auto_resume claim` 91 tests **exit=0**; `runner_patterns tmp_hygiene stdlib_only` exit=0. 임시 잔재 0(픽스처 close + tmproot).

### 주의 — 동시 편집 중인 bin/s9

12회 반복 중 2회 setUpClass 오류가 났는데 둘 다 픽스처가 아니라 리드가 지금 고치는 중인 bin/s9(`git status` M, 157+/12-)가 원인입니다. 한 번은 s9 프로세스가 `SyntaxError: expected ':' (bin/s9:10329)` 로 죽었고, 한 번은 `s9 new` 가 rc 0 에 빈 출력을 냈습니다(같은 시각대 중간 상태로 추정, 재현 불가). 픽스처는 이 경우를 삼키지 않도록 `new_request` 가 rc·stdout·stderr·vault 목록·sync.log 를 담아 실패로 올립니다. bin/s9 가 멈춘 트리에서 돌리면 결정적입니다(10/10 동일).

### 한계

- 016/020 뒤 S3 는 alpha 세션 A 의 리스가 신선하다는 전제로 0 이어야 합니다 — 리스 TTL 을 바꾸면 이 씨앗(방금 전이)이 그 전제를 만족하는지 확인이 필요합니다.
- S6 은 「beta 가 Y 를 재개하지 않는다」까지만 단정합니다. blocked_by 목록 정리를 어느 머신이 해도 되는지는 016 이 정합니다.
- 실행 시간 대부분은 문서 이벤트마다 도는 실제 commit→pull→push 입니다(클래스당 ~3s).

REQ 문서에는 `--label tdd`(시나리오 S1~S7)와 `--label response`(파일·실측·안정성) 노트를 남겼습니다.

### 2026-09-02T15:44:31+09:00 commit (by nicehugepark)

커밋 c996481 — 두 머신 위장 시험 픽스처 — 현행 결함 5건을 빨간 시험으로 고정 (REQ-20260902-015-62x6)

## History
- 2026-09-02T14:06:34+09:00 created by nicehugepark (status: open)
- 2026-09-02T15:25:36+09:00 status: open -> in-progress (by nicehugepark) — 착수 — 015 는 quality-assurance 위임(tests/ 한정), 029·033 은 리드 직접(bin/s9)
- 2026-09-02T15:45:37+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: tests/twomachine.py 픽스처(S9_MACHINE alpha/beta + bare origin 왕복, fake_spawn)와 test_two_machine.py 8건 — S1·S2 픽스처 정상, S3 회귀 고정, S4~S7 현행 결함이 빨강(expectedFailure 로 스위트는 녹색, 고치면 unexpected success 로 제거 강제). 리드 실측 exit 0, 커밋 c996481. quality-assurance 위임 산출
