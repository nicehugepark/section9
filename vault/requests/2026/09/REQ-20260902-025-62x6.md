---
id: REQ-20260902-025-62x6
type: request
title: 동기화 멈춤 표시와 복구
summary: git 판·digest 첫 줄·훅 주입에 「마지막 보냄/받음·대기 n건·diverged」. 60초 초과 주황, 5분 초과 붉은 줄 + 리드 1회 주입. s9 sync resolve <id> --take mine|theirs, 드라이버가 못 푼 충돌은 양쪽 본을 state/sync-conflict/ 에 보존. reset --hard 금지
goal: 연속 실패가 화면·digest 에 다음 행동과 함께 뜨고 resolve 명령으로 복구되며 미전송 커밋은 유실되지 않는다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 67e42231
agents: ["sub:backend-developer:a18e0c9c", "sub:backend-developer"]
contributions: [{"actor": "sub:backend-developer:a18e0c9c", "item": "025 동기화 멈춤 표시와 복구", "started": "2026-09-02T16:30:32+09:00", "ended": "2026-09-02T16:47:20+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/67e42231-60e4-4bcc-b051-5484fe7ffbb9/tasks/a18e0c9cc977f54b0.output"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (tests/test_sync_status.py — bare origin + 클론 둘 실측, S9_ROOT 격", "started": "2026-09-02T16:34:27+09:00", "ended": "2026-09-02T16:34:27+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 — 검증 결과 (python3 tests/ sync_status → 16 tests OK, exit 0; 이웃", "started": "2026-09-02T16:46:58+09:00", "ended": "2026-09-02T16:46:58+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "구현 완료 (커밋·전이는 리드 몫).", "started": "2026-09-02T16:47:00+09:00", "ended": "2026-09-02T16:47:00+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:a18e0c9c", "item": "`[2026-09-02 16:47:10 KST - backend-developer]`", "started": "2026-09-02T16:47:20+09:00", "ended": "2026-09-02T16:47:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/67e42231-60e4-4bcc-b051-5484fe7ffbb9/subagents/agent-a18e0c9cc977f54b0.jsonl"}]
project: section9
parent: REQ-20260902-014-62x6
relates_why: {}
tags: ["sync", "audit", "docs", "attached", "png", "image"]
created: 2026-09-02T14:06:37+09:00
status_since: 2026-09-02T16:48:27+09:00
updated: 2026-09-02T16:48:27+09:00
priority: 50
---

## Original

설계: DOC-20260902-001-62x6 §2 축3. 선행: 전송 비동기화와 수신 폴링

## Notes

### 2026-09-02T16:34:27+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (tests/test_sync_status.py — bare origin + 클론 둘 실측, S9_ROOT 격리)
- [ ] S1. sync_status 기본: remote 모드, push 를 마친 클론 → pending 0 · stalled_sec 0 · kind '' · diverged False · level ''.
- [ ] S2. pending·stalled: 미전송 커밋 1개(작성 시각 10분 전으로 위장) → pending 1 · stalled_sec ≥ 600 · level 'stale'; 작성 시각이 방금이면 level ''.
- [ ] S3. last_sent/last_received: sync.jsonl 의 마지막 push rc=0 → last_sent, state/.sync-pull.ts → last_received; 둘 다 없으면 0.
- [ ] S4. kind: 마지막 실패(kind=conflict)가 마지막 성공보다 늦으면 kind='conflict', 성공이 더 늦으면 ''.
- [ ] S5. diverged: 로컬 커밋 + 원격의 새 커밋(fetch 뒤) → diverged True.
- [ ] S6. local 모드 → mode 'local', stalled_sec 0 (밖으로 나갈 일이 없으니 멈춤도 없다).
- [ ] S7. 문구 고정점: sync_status_line → '마지막 보냄 12초 전 · 받음 8초 전 · 대기 3건'; 60초 미만 level '' · 60~300 'late' · 300 초과 'stale'; 보낸 적 없으면 '없음'.
- [ ] S8. git_state()['sync'] 가 sync_status 결과(line·level 포함)를 그대로 싣는다.
- [ ] S9. cmd_digest 머리: level 있으면 첫 줄이 '◈ 동기화 지연' 로 시작하고 다음 행동을 담는다; level 없으면 그 줄이 없다.
- [ ] S10. 's9 sync --status --quiet': stale 이면 한 줄 1회(같은 에피소드의 두 번째 호출은 빈 출력), late/정상은 빈 출력, 밀린 커밋이 바뀌면 다시 1회.
- [ ] S11. resolve --take mine: 두 클론이 같은 문서를 달리 고침 → A 에서 resolve mine → rebase 완료, 파일 내용=A 것, state/sync-conflict/<id>.mine.md·<id>.theirs.md 보존, 잔재(.git/rebase-merge) 없음.
- [ ] S12. resolve --take theirs: 파일 내용=B 것, 이후 push 가 통과(ahead 0).
- [ ] S13. resolve 실패 경로: --take 가 mine|theirs 밖이면 ValueError; 지목하지 않은 다른 문서가 충돌하면 abort 하고 그 id 를 오류에 담는다(잔재 없음).
- [ ] S14. rescue: 미전송 커밋 2개 → state/sync-rescue/*.patch 2개, 새 클론에 git am 으로 재적용해 내용 동일; 미전송 0 → 빈 목록; 재실행해도 앞 파일을 덮지 않는다.
- [ ] S15. 회귀(게이트): 새 코드 구역에 'reset --hard'·'checkout --'·'stash' 호출이 없다(소스 검사).
- [ ] S16. 화면 고정점: repo.js 가 sync.line 을 #g-sync 에 그리고 level 을 late/stale 글자색 클래스로만 쓴다(소스 검사) + s9 shot 캡처(?git=stale).
- [ ] S17. 훅 고정점: bin/s9-audit-prompt 가 'sync --status --quiet' 를 부르고 접두 '◈ 동기화 지연' 으로 주입한다(소스 검사).

### 2026-09-02T16:46:58+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 — 검증 결과 (python3 tests/ sync_status → 16 tests OK, exit 0; 이웃 스위트 sync·git_panel·audit_prompt·hook 전부 OK)
- [x] S1. sync_status 기본: pending 0 · stalled 0 · level '' · diverged False
- [x] S2. pending·stalled: 작성 시각 10분 전 커밋 → stale, 방금 → '', 2분 전 → late
- [x] S3. last_sent(sync.jsonl push rc=0)·last_received(.sync-pull.ts), 없으면 0
- [x] S4. kind: 마지막 실패가 마지막 성공보다 늦을 때만
- [x] S5. diverged: 로컬 커밋 + fetch 된 원격 커밋
- [x] S6. local 모드는 stalled 0
- [x] S7. 문구 고정점 '마지막 보냄 12초 전 · 받음 8초 전 · 대기 3건' + 갈림·종류 꼬리
- [x] S8. git_state()['sync'] 동일 함수 (demo=stale 포함)
- [x] S9. cmd_digest 머리 '◈ 동기화 지연 — … — 다음 행동' (멈춤일 때만)
- [x] S10. 's9 sync --status --quiet' 같은 멈춤에 1회, 에피소드 바뀌면 다시 1회
- [x] S11. resolve --take mine: 내용=A, sync-conflict/<id>.mine.md·.theirs.md 보존, 잔재 없음, push ok, pending 0
- [x] S12. resolve --take theirs: 내용=B, B 가 pull 해도 같은 본
- [x] S13. 잘못된 take ValueError · 지목 안 한 문서 충돌 → abort + others, 둘 다 지목하면 통과
- [x] S14. rescue: patch 2개 → 새 클론 git am 재적용 동일, 재실행 번호 이어짐(덮지 않음), 커밋 보존
- [x] S15. 새 구역에 --hard/checkout/stash/restore/clean 없음
- [x] S16. repo.js #g-sync + late/stale 글자색 클래스 (소스) + s9 shot 캡처(?git=late 주황, ?git=stale 붉은)
- [x] S17. 훅이 sync --status --quiet 를 부르고 '◈ 동기화 지연' 으로 주입

### 2026-09-02T16:47:00+09:00 response (agent: sub:backend-developer, by nicehugepark)

구현 완료 (커밋·전이는 리드 몫).

**한 함수** bin/s9 sync_status() — sync.jsonl(마지막 push/pull 성공·마지막 실패 종류)·state/.sync-pull.ts(받음)·.sync-queue·.sync-fail.ts(백오프)·git rev-list @{u}...HEAD(pending·diverged)·git log %at(가장 오래된 미전송 커밋의 작성 시각 = stalled_sec; rebase 가 갈아 끼우는 커밋 시각이 아니라 작성 시각이라 rebase 뒤에도 나이가 안 줄어든다)·.git/rebase-merge 잔재(diverged+conflict). level: 60초↑ late · 5분↑ stale. remote 가 아니면 stalled 0(나갈 일이 없으면 멈춤도 없다). oldest(미전송 첫 커밋 해시)가 훅 1회 주입의 에피소드 키.
문장은 sync_status_line() 하나 — '마지막 보냄 12초 전 · 받음 8초 전 · 대기 3건' + (갈림) '· 갈래가 갈렸습니다' + (실패 종류) SYNC_KIND_WORD. 다음 행동은 sync_status_hint().

**표면 셋**
① git 판: git_state() 가 st['sync'](line·level 포함)를 싣고, web/app/repo.js gitSyncPaint 가 #g-sync 에 글자만 그린다 — .wfact.gsync.late=var(--c-review) 주황, .stale=var(--c-blocked) 붉은(web/css/density.css). 면 없음. remote 가 아니면 줄이 서지 않는다. 판의 기존 '갈래가 갈렸습니다.' 사실 줄은 동기화 줄이 설 때 물러난다(캡처에서 두 번 서던 것). 캡처 갈래 ?git=late · ?git=stale 추가(GIT_DEMO).
② s9 digest 머리: level 있을 때만 첫 줄 '◈ 동기화 지연 — <줄> — <다음 행동>'.
③ 훅: bin/s9-audit-prompt 가 's9 sync --status --quiet' 한 번 불러 stale 일 때만 ' ◈ 동기화 지연: …' 주입(request 갈래). --quiet 는 state/.sync-stall-seen 에 에피소드 키를 적어 같은 멈춤에 1회만 낸다.

**복구**
- s9 sync resolve <id> [<id>…] --take mine|theirs: 잔재 abort → fetch → rebase --autostash @{u} → 충돌마다 지목 문서를 index 에서 읽어(git show :3:=내 것, :2:=origin) 파일로 쓰고 add → GIT_EDITOR=true rebase --continue 반복. 고르기 전에 양쪽 본을 state/sync-conflict/<id>.mine.md·.theirs.md 로 보존. 지목하지 않은 문서가 충돌하면 abort 하고 그 id 를 돌려준다(사람이 안 고른 것을 기계가 고르지 않는다). 끝나면 sync_run('resolve')로 바로 민다(serve 있으면 큐). sync_run 과 같은 .sync.lock 을 잡아 전송 루프와 겹치지 않는다. checkout --·reset --hard·stash 없음(S15 가 소스로 감시).
- s9 sync rescue: git format-patch @{u}..HEAD → state/sync-rescue/NNNN-*.patch, 번호는 기존 다음부터(덮지 않음). 되살리기 git am.
- s9 sync --status 에 remote 면 상태 줄 + [level] → 다음 행동 한 줄 추가.

**시험** tests/test_sync_status.py — bare origin + 클론 둘 실측 16건, 5~7초. 이웃 sync·git_panel·audit_prompt·hook 스위트 OK.
**서버** :9909 재기동(kill -TERM → serve-guard 재기동) 후 /api/git/state?git=stale 로 sync 필드 실측.

**한계·남긴 것**
- SYNC_KIND_WORD 네 낱말('거부됨 — 다시 밉니다'·'충돌 — s9 sync resolve 필요'·'연결 안 됨'·'보호 규칙에 막힘 — PR 필요')은 화면 문구라 ux-writer·tech-writer 판정 대상.
- 병합 드라이버(REQ-024)가 들어오면 resolve 가 만나는 충돌은 드라이버가 못 푼 것만이다 — 지금 시험은 드라이버 없이 순수 git 충돌로 잰다.
- 훅 주입은 request 갈래에만; question 갈래에는 넣지 않았다(같은 파일 두 자리 수정 회피). 필요하면 한 줄 추가.
- s9 shot 이 ERR_CONNECTION_RESET 을 간헐로 낸다(WSL 포트 중계, serve.log port-guard 59%) — 재시도로 캡처.

[Image: assets/REQ-20260902-025-62x6/sync-late.png]
[Image: assets/REQ-20260902-025-62x6/sync-stale.png]
[Image: assets/REQ-20260902-025-62x6/sync-split.png]

### 2026-09-02T16:47:20+09:00 subagent (agent: sub:backend-developer:a18e0c9c, by nicehugepark)

`[2026-09-02 16:47:10 KST - backend-developer]`

REQ-20260902-025-62x6 구현 완료 — 전이·커밋은 리드 몫.

- 한 함수 `sync_status()`(bin/s9, sync_transport_tick 바로 아래): mode·last_sent·last_received·pending·stalled_sec(가장 오래된 미전송 커밋의 작성 시각)·kind·diverged·level(60s late/5min stale)·oldest. 문장은 `sync_status_line()` 하나.
- 표면 셋: git 판 `st["sync"]` → repo.js `#g-sync` 글자색만(density.css late=주황·stale=붉은, 캡처 `?git=late|stale`) · `s9 digest` 머리 `◈ 동기화 지연` (멈춤일 때만) · 훅 `s9 sync --status --quiet` → 같은 멈춤에 1회.
- 복구: `s9 sync resolve <id>… --take mine|theirs`(양쪽 본 state/sync-conflict/ 보존, 미지목 충돌은 abort+id 반환, index 에서 읽어 씀 — checkout/reset/stash 없음) · `s9 sync rescue`(format-patch, 번호 이어짐).
- 시험: `/home/sjpark1/section9/tests/test_sync_status.py` 16건 OK(exit 0), 이웃 sync·git_panel·audit_prompt·hook OK. 서버 재기동 후 API·캡처 실측. 노트(tdd 체크·response+캡처 3장) 남김.
- 한계: KIND 낱말 넷은 ux-writer 판정 대상; 훅 주입은 request 갈래만.

## History
- 2026-09-02T14:06:37+09:00 created by nicehugepark (status: open)
- 2026-09-02T16:29:24+09:00 status: open -> in-progress (by nicehugepark) — 병렬 착수 — 위임(구역 분리: 025 sync 표면화, 026·027 바인딩/지문, 028·030 write_doc 게이트, 044 워커 프롬프트)
- 2026-09-02T16:48:27+09:00 status: in-progress -> done (by nicehugepark) — goal 충족(리드 검증): sync_status() 한 함수를 git 판(글자색만 — 캡처 sync-stale.png 확인)·digest 머리·훅 주입이 함께 보고, 연속 실패가 「마지막 보냄·받음·대기 n건·갈래」로 뜨며, s9 sync resolve --take mine|theirs 와 s9 sync rescue 로 복구·보존된다(reset --hard 없음). 시험 sync_status 16건 + sync 이웃 39건 OK. backend-developer 위임 산출
